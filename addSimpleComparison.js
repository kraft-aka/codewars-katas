// Write a function that will compare two values, one will be a number and one
// will be a string. Return true if they are the same character (regardless of
// their different data types) and return false if they are not.

const add = (a, b) => a == b;
